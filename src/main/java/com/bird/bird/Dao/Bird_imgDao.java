package com.bird.bird.Dao;

import com.bird.bird.entity.Bird_img;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Bird_imgDao extends JpaRepository<Bird_img,Integer> {
}
