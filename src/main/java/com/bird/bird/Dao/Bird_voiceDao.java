package com.bird.bird.Dao;

import com.bird.bird.entity.Bird_voice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Bird_voiceDao extends JpaRepository<Bird_voice,Integer> {
}
